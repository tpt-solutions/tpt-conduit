# /web — Frontend (Phase 4)

Planned: Next.js (React) app with GraphQL client wiring, ticket list/detail/
create views, workflow-run timeline visualization, approval action UI,
white-label theming, a plugin architecture, and basic-auth login.
