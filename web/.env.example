# Backend GraphQL endpoint (server-side only — never exposed to the browser)
CONDUIT_API_URL=http://localhost:8080

# White-label theming (safe to expose to the browser)
NEXT_PUBLIC_BRAND_NAME=TPT Conduit
NEXT_PUBLIC_BRAND_LOGO_URL=
NEXT_PUBLIC_COLOR_PRIMARY=#2451ff
NEXT_PUBLIC_COLOR_ACCENT=#12b886
